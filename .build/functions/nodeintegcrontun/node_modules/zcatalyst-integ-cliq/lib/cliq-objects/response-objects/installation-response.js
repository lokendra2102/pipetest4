export default class InstallationResponse {
    constructor() {
        this.status = 200;
    }
    addNote(...note) {
        if (this.note === undefined) {
            this.note = note;
            return this.note.length;
        }
        return this.note.push(...note);
    }
}
