import { Handler } from '../../handler.js';
import CONSTANTS from '../../constants.js';
import InstallationResponse from '../response-objects/installation-response.js';
export default class installationValidator {
    validateInstallation(handler) {
        Handler.registerHandler(CONSTANTS.INSTALLATION_VALIDATOR, CONSTANTS.HANDLERS.installation_validator, handler, InstallationResponse);
    }
    newInstallationResponse() {
        return new InstallationResponse();
    }
}
