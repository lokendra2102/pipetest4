import FormValue from './form-value.js';
export default class FormDynamicFieldResponse {
    options?: Array<FormValue>;
    newFormValue(label?: string, value?: string): FormValue;
    addOption(...option: Array<FormValue>): number;
}
