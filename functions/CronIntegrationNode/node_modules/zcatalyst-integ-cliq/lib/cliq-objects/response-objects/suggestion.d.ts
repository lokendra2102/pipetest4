import { SuggestionObject as ISuggestionObject } from '../common.js';
export default class SuggestionList {
    list?: Array<SuggestionObject>;
    newSuggestionObject(text?: string, icon?: string): SuggestionObject;
    addSuggestion(...suggestion: Array<SuggestionObject>): number;
}
export declare class SuggestionObject implements ISuggestionObject {
    text?: string;
    icon?: string;
    constructor(text?: string);
}
