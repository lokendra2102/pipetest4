import { BANNER_STATUS, File, Mention, MESSAGE_TYPE, Message as IMessage } from '../common.js';
export declare class Message implements IMessage {
    type?: MESSAGE_TYPE;
    mentions?: Array<Mention>;
    text?: string;
    file?: File;
    comment?: string;
    status?: BANNER_STATUS;
    newMention(): Mention;
    addMentions(...mention: Array<Mention>): number;
}
