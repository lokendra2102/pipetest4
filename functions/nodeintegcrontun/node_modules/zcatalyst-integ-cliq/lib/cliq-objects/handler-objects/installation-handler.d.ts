import { AppInfo, User } from '../common.js';
import InstallationResponse from '../response-objects/installation-response.js';
export interface InstallationRequest {
    user: User;
    app_info: AppInfo;
}
export default class InstallationHandler {
    handleInstallation(handler: (req: InstallationRequest, res: InstallationResponse, ...args: Array<unknown>) => Promise<InstallationResponse | undefined>): void;
    newInstallationResponse(): InstallationResponse;
}
