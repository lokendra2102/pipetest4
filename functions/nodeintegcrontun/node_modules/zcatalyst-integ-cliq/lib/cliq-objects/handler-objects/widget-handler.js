import { Handler } from '../../handler.js';
import CONSTANTS from '../../constants.js';
import WidgetResponse from '../response-objects/widget-response.js';
const { WIDGET } = CONSTANTS;
const { widget_handler } = CONSTANTS.HANDLERS;
export default class WidgetHandler {
    viewHandler(handler) {
        Handler.registerHandler(WIDGET, widget_handler.VIEW_HANDLER, handler, WidgetResponse);
    }
    newWidgetResponse() {
        return new WidgetResponse();
    }
}
