import FormInput from './form-input.js';
export default class Form {
    constructor() {
        this.type = 'form';
    }
    newFormActionsObject() {
        return new FormActionsObject();
    }
    newFormAction(name) {
        return new FormAction(name);
    }
    newFormInput() {
        return new FormInput();
    }
    addInputs(...input) {
        if (this.inputs === undefined) {
            this.inputs = input;
            return this.inputs.length;
        }
        return this.inputs.push(...input);
    }
}
class FormActionsObject {
    newFormAction(name) {
        return new FormAction(name);
    }
}
class FormAction {
    constructor(name) {
        this.type = 'invoke.function';
        this.name = name;
    }
}
